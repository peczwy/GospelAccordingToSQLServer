import 'package:flutter/material.dart';
import 'package:gospel_at_flutter/gospel_at_flutter.dart';

void main() {
  /// Suprisingly URL Routing strategy produces refresh errors
  // usePathUrlStrategy();
  runApp(const GospelApp());
}
